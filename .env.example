# Example .env file for Discord-Bot
# Copy this to a file named .env at project root and fill the values.

# Discord bot token (required)
DISCORD_TOKEN=your_discord_bot_token_here

# Pexels API key (optional - required if you want to use the `img` command)
PEXELS_API_KEY=your_pexels_api_key_here

# Optional: set the bot prefix if your main.py supports it (example)
# BOT_PREFIX=!

# Security: never commit your real .env file with secrets to a public repo.
